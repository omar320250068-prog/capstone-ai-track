{
  "name": "settings-form-precise",
  "version": "1.0.0",
  "private": true,
  "description": "Settings form with a validated, testable pure validation module (round two).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}