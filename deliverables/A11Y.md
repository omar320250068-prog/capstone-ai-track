# Track 3 — Phase: Foundations · Accessibility · Deliverables

Repo: https://github.com/omar320250068-prog/capstone-ai-track
Branch: `feature/a11y-playground`

## Deliverable

| Item | Location |
| --- | --- |
| Kind: hand-built dialog, tabs, disclosure (React + TS, no libraries) | `playground/app/src/components/` |
| Keyboard tests | `playground/app/src/components/__tests__/` |
| Unit tests | 19/19 passing (`playground/app` — `npm test`) |
| Lint / build | Clean (oxlint, `tsc -b && vite build`) |
| shadcn/ui dialog + tabs generated source | `playground/shadcn-comparison/src/components/ui/` |
| Comparison notes (≥2 concrete gaps) | `playground/NOTES.md` |

## Evaluation criteria

| Criterion | Status | Evidence |
| --- | --- | --- |
| All three operate fully by keyboard (Tab, Escape, arrows) | Done | Playwright keyboard-only run (14 steps, all pass) + unit tests |
| Modal traps focus and returns it on close | Done | `dialog.test.tsx` + Playwright: 8×Tab trapped, Shift+Tab wraps, Escape closes → focus back on trigger |
| Notes name ≥2 concrete gaps vs shadcn | Done | `NOTES.md` — scroll lock, visibility-aware trap focus, vertical/RTL tabs, panel tabindex, nesting |
| TS compiles with no `any` escapes in component props | Done | `"strict": true`; explicit `type`/`ComponentProps` prop types in `playground/app/src/components/*` |

## Notes

- WAI-ARIA APG is the spec: Modal Dialog, Tabs ("automatic activation"),
  Disclosure.
- The honest headline from the comparison: shadcn's generated files are thin
  styling wrappers — the a11y behaviour is delegated to the `radix-ui`
  primitives, which is exactly the reviewable/open-source quality the brief
  highlights. My hand-built versions use `inert`, focus trap, focus return.

Saved from an opencode session running in `C:\trak nu, 3`.